name: style_transfer_app
channels:
- defaults
- conda-forge
dependencies:
  - streamlit
  - numpy
  - pytorch
  - jupyter
  - pip:
      - torchvision
      - torchaudio
      - pytorch-cuda=11.6 -c pytorch -c nvidia